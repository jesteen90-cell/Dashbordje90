(()=>{
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const posOrder={FWD:1,MID:2,DEF:3,GK:4};
function pcard(p){const tag=p.optimal_captain?' <span class="status do">C</span>':p.optimal_vice?' <span class="status consider">VC</span>':'';return `<div class="card" style="padding:10px;text-align:center;min-width:0"><b>${esc(p.name)}${tag}</b><div class="muted">${esc(p.team||'')} · ${esc(p.fixture||'')}</div><div style="margin-top:5px"><b>${Number(p.xp||0).toFixed(1)} p</b> <span class="muted">forventet</span></div>${p.news?`<div class="muted" style="margin-top:4px">${esc(p.news)}</div>`:''}</div>`}
function lineupHtml(d){const o=d.optimal_current_lineup||{},cs=d.current_squad||{};if(o.version!=='1.0-xp-legal-xi'||cs.version!=='1.0-official-transfers')return'';const rows=['FWD','MID','DEF','GK'].map(pos=>{const xs=(o.lineup||[]).filter(p=>p.position===pos);if(!xs.length)return'';return `<div style="display:grid;grid-template-columns:repeat(${xs.length},minmax(0,1fr));gap:7px;margin-top:8px">${xs.map(pcard).join('')}</div>`}).join('');const bench=(o.bench||[]).map((p,i)=>`<div class="card" style="padding:9px"><div class="eyebrow">${p.position==='GK'?'Reservekeeper':`Benk ${i+1}`}</div><b>${esc(p.name)}</b><div class="muted">${esc(p.team||'')} · ${esc(p.fixture||'')} · ${Number(p.xp||0).toFixed(1)} forventede p</div></div>`).join('');const tx=Number(cs.transfer_count_current_gw||0);return `<div class="section" id="optimalCurrentSection"><div class="tabExplain"><b>OPTIMAL XI = beste lag fra troppen du eier nå</b><span>Bygget fra siste bekreftede 15 + offisielle transfers i inneværende runde. Velger den lovlige startelleveren med høyest forventet GW-score.</span></div><h2>Optimal startellever – nåværende tropp</h2><div class="card"><div class="planrow"><div><div class="eyebrow">GW ${esc(o.gw)}</div><b>Formasjon ${esc(o.formation||'—')}</b></div><div style="text-align:right"><b>${Number(o.expected_team_score||0).toFixed(1)} forventede p</b><div class="muted">startelleveren samlet</div></div></div><div class="miniStats" style="margin-top:9px"><div class="mini"><b>${esc(o.captain||'—')}</b><span>kaptein</span></div><div class="mini"><b>${esc(o.vice||'—')}</b><span>visekaptein</span></div><div class="mini"><b>${tx}</b><span>registrerte GW-transfers</span></div></div><div class="muted" style="margin-top:8px">Dette er anbefalt oppstilling, ikke en påstand om at FPL-laget ditt allerede er satt slik.</div></div><div style="margin-top:8px">${rows}</div><h2 style="margin-top:18px">Anbefalt benkerekkefølge</h2><div style="display:grid;gap:7px">${bench}</div></div>`}
async function run(){try{const r=await fetch(`data.json?lineup=${Date.now()}`,{cache:'no-store'});if(!r.ok)return;const d=await r.json(),team=document.getElementById('team');if(!team||document.getElementById('optimalCurrentSection'))return;const html=lineupHtml(d);if(!html)return;const note=document.getElementById('sourceNote');if(note)note.insertAdjacentHTML('afterend',html);else team.insertAdjacentHTML('afterbegin',html)}catch(_e){}}
run();
})();